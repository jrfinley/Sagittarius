using UnityEngine;
using System.Collections;

public class Collisioned : MonoBehaviour
{
    // For intro scene, if the player collides to the ground from a fallen edge,
    // then the dialog window will display, and the oracle will aproach the player. 

    // once the orcale approaches the player, she will ask if they are ready to move on. 
    // A message will be prompted to be taught how to move the player, then will encounter a quest item.

    // etc... 
	
}
